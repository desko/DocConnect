import Section from '../components/Section/Section';
import AppointmentsController from '../components/AppointmentsController/AppointmentsController';

const Appointments = () => {
  return (
    <Section title={'Appointments'}>
      <AppointmentsController/>
    </Section>

  );
};

export default Appointments;
